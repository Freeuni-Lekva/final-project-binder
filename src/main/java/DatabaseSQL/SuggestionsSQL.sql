Create table Suggestions(
                            id int auto_increment primary key,
                            username_getter  varchar(30),
                            username_suggested varchar(30)
);
